server:
  port: 8081
  servlet:
    context-path: /luckymoney

limit:
  minMoney: 0.01
  maxMoney: 100
  description: 最小红包为${limit.minMoney}元,最多${limit.maxMoney}元

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://47.100.38.57:3306/blog
    username: root
    password: Leiyiming417.
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true